Задача 3: "Троттлинг при движения мыши"

Реализуй поведение, при котором:
1. При движении мыши по странице (mousemove) в элементе <div id="coords"> отображаются текущие координаты мыши (x, y).
2. Но координаты обновляются не чаще, чем 1 раз в 500 мс (используй троттлинг).

HTML:
<div id="coords" style="padding: 1em; border: 1px solid #ccc; width: max-content;">
  Перемести мышь
</div>
